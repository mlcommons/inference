# User configuration for MLPerf Loadgen
# The format of this config file is 'key = value'.
# The key has the format 'model.scenario.key'. Value is mostly int64_t.
# Model maybe '*' as wildcard. In that case the value applies to all models.
# All times are in milliseconds

# E2E workload configuration
# For multi-hop RAG queries with iterative retrieval

# Offline scenario settings
# Target QPS based on observed throughput: 824 queries / 7200 seconds ≈ 0.11 QPS
e2e.Offline.target_qps = 0.11
e2e.Offline.min_duration = 60000
e2e.Offline.min_query_count = 50
